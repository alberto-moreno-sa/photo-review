import { colors, space, typography, body, zIndex, breakpoints } from './styles';

// eslint-disable-next-line import/no-anonymous-default-export
export default {
  space,
  colors,
  typography,
  body,
  zIndex,
  breakpoints,
};
